{
  "name": "ruff-hooks",
  "version": "1.0.0",
  "description": "Runs ruff check (with auto-fix) and ruff format on Python files after every Write, Edit, or MultiEdit. Blocks Claude on unfixable linting violations. Merges with your project's ruff config if present, falls back to opinionated defaults otherwise.",
  "author": {
    "name": "Airis"
  },
  "license": "MIT",
  "keywords": ["python", "ruff", "linting", "formatting", "hooks"],
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/ruff_check.py"
          },
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/ruff_format.py"
          }
        ]
      }
    ]
  }
}
